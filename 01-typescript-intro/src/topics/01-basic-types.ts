// el presente archivo es un módulo

const name: string = 'Strider';
// acá para que la variable asimile diferentes tipos de datos, se les puede separar mediante el caracter que concatena en sql
let hpPoints: number | string = 95;

hpPoints = 'FULL';


export {};